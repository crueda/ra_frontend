import { mount } from '@vue/test-utils'
//import MainComponent from '../../../../src/components/MainComponent.vue'
import DebtsList from '../../../../src/components/debts/DebtsList.vue'

// Aquí hay algunas pruebas de Jasmine 2.0, aunque puede
// usar cualquier librería que prefiera
describe('DebtsList', () => {
  // Inspeccionar las opciones de componentes en bruto
  it('tiene un hook creado', () => {
    expect(typeof DebtsList.mounted).toBe('function')
  })
})
